/**
 * @deprecated
 * Bu script artık kullanılmamaktadır. 
 * 'quiz-generator' Edge Function'ı kaldırılmış, yerine istemci taraflı motor getirilmiştir.
 */
console.warn("⚠️ trigger-pending.js artık desteklenmiyor. Soru üretimi tarayıcı üzerinden otomatik yapılmaktadır.");
process.exit(0);
